services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: sketch
      POSTGRES_USER: sketch
      POSTGRES_PASSWORD: sketch
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
